rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read access
      allow read: if true;
      
      // Allow write access only to authenticated users
      // You can further restrict this to specific paths or file types
      allow write: if request.auth != null;
    }
  }
}
